
// Logging information
const infor = (...message) => {
  console.log(...message)
}

// Logging error information
const error = (...errorMessage) => {
  console.log(...errorMessage)
}

export default {
  infor,
  error
}